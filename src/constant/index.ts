export enum SessionType {
  hot = 'hot',
  hugebonus = 'hugebonus',
  hightwrate = 'hightwrate',
  free = 'free'
}

export enum FreeType {
  daily = 'daily',
  weekly = 'weekly',
  monthly = 'monthly'
}